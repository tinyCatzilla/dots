"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.DocumentPadder = void 0;
class DocumentPadder {
    static pad(content, line, char) {
        let text = '';
        for (let i = 0; i < line - 1; i++) {
            text += "\n";
        }
        text += ' '.repeat(char);
        text += content;
        return text;
    }
    static substitute(content, char) {
        let text = '';
        for (let i = 0; i < content.length; i++) {
            const sChar = content[i];
            if (sChar == "\n") {
                text += sChar;
            }
            else {
                text += char;
            }
        }
        return text;
    }
}
exports.DocumentPadder = DocumentPadder;
//# sourceMappingURL=documentPadder.js.map