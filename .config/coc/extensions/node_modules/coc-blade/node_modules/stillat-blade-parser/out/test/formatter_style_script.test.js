"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const assert_1 = __importDefault(require("assert"));
const utils_1 = require("../formatting/prettier/utils");
suite('Basic <style> and <script> Formatting', () => {
    test('it can format echo and directives inside style and script tags', () => {
        assert_1.default.strictEqual((0, utils_1.formatBladeString)(`
            <html>
            <style>
                .thing {
                    background-color: 
                    {{
                                     $color            }}
                                                        }
                                                    </style>
                                                    {{ $color }}
            <style>
                .thing {
                    background-color: {{ $color   + $something +                 $otherThing 
                    
                    
                    
                                    }}
                }
            </style>
            <body>
            
                                <script>
                                var test = @js(         'something'             )
                                </script>
                    @js('something')
                                            <script>
                                            var test = @js(   'something'      )
                                            </script>
                                            </body>
            
                                            </html>`).trim(), `<html>
    <style>
        .thing {
            background-color: {{ $color }};
        }
    </style>
    {{ $color }}
    <style>
        .thing {
            background-color: {{ $color + $something + $otherThing }};
        }
    </style>
    <body>
        <script>
            var test = @js("something");
        </script>
        @js("something")
        <script>
            var test = @js("something");
        </script>
    </body>
</html>`);
    });
    test('it does not continue to indent', () => {
        const input = `
<style>
    :root {
        --foo: red;

        @if (true)
            --bar: blue;
        @endif
    }
</style>
`;
        const expected = `<style>
    :root {
        --foo: red;

        @if (true)
            --bar: blue;
        @endif
    }
</style>
`;
        let out = (0, utils_1.formatBladeString)(input);
        assert_1.default.strictEqual(out, expected);
        for (let i = 0; i < 5; i++) {
            out = (0, utils_1.formatBladeString)(out);
            assert_1.default.strictEqual(out, expected);
        }
    });
    test('it does not do really dumb things if already indented nicely', () => {
        const input = `
<style>
    :root {
        --foo: red;

        @if (true)
            --bar: blue;
        @endif
    }
</style>

<div>
    <style>
        :root {
            --foo: red;

            @if (true)
                --bar: blue;
            @endif
        }
    </style>
</div>
`;
        const out = `<style>
    :root {
        --foo: red;

        @if (true)
            --bar: blue;
        @endif
    }
</style>

<div>
    <style>
        :root {
            --foo: red;

            @if (true)
                --bar: blue;
            @endif
        }
    </style>
</div>
`;
        assert_1.default.strictEqual((0, utils_1.formatBladeString)(input), out);
        assert_1.default.strictEqual((0, utils_1.formatBladeString)(out), out);
    });
});
//# sourceMappingURL=formatter_style_script.test.js.map