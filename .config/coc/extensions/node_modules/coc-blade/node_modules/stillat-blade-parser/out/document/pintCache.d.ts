export interface IRestoredCacheResults {
    contentMapping: Map<string, string>;
    resultMapping: Map<string, string>;
}
export declare class PintCache {
    private cacheDir;
    private cacheVersion;
    constructor(cacheDir: string);
    private generateSHA1Hash;
    private getCachePath;
    canCache(path: string): boolean;
    has(path: string): boolean;
    private serializeMap;
    private unserializeMap;
    put(path: string, resultMapping: Map<string, string>, contentMapping: Map<string, string>): void;
    get(path: string): IRestoredCacheResults;
    isValid(cachedContent: Map<string, string>, templateContent: Map<string, string>): boolean;
}
//# sourceMappingURL=pintCache.d.ts.map