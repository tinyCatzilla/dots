"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.ErrorPrinter = void 0;
class ErrorPrinter {
    static printError(error, lines) {
        var _a, _b;
        let result = error.message + "\n\n", line = (_b = (_a = error.node) === null || _a === void 0 ? void 0 : _a.startPosition) === null || _b === void 0 ? void 0 : _b.line;
        let maxLine = line;
        lines.forEach((text, lineNumber) => {
            maxLine = lineNumber;
        });
        let maxLineLen = maxLine.toString().length;
        lines.forEach((lineText, lineNumber) => {
            let curLineString = lineNumber.toString(), prefix = '', linePaddingLen = 0;
            linePaddingLen = maxLineLen - curLineString.length;
            if (line == lineNumber) {
                prefix = ' >' + '0'.repeat(linePaddingLen) + curLineString + '| ';
            }
            else {
                prefix = '  ' + '0'.repeat(linePaddingLen) + curLineString + '| ';
            }
            result += prefix + lineText + "\n";
        });
        return result;
    }
}
exports.ErrorPrinter = ErrorPrinter;
//# sourceMappingURL=errorPrinter.js.map