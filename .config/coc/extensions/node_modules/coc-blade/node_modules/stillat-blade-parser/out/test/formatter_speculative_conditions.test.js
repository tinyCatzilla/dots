"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const assert_1 = __importDefault(require("assert"));
const utils_1 = require("../formatting/prettier/utils");
suite('Speculative Conditions Formatting', () => {
    test('it can format detected custom conditions', () => {
        assert_1.default.strictEqual((0, utils_1.formatBladeString)(`@disk('local')
    <div>
    @disk('local')
    @disk('local')
    <!-- The application is using the local disk... -->
@elsedisk('s3')
    <!-- The application is using the s3 disk... -->
@else
    <!-- The application is using some other disk... -->
@enddisk
@elsedisk('s3')
    <!-- The application is using the s3 disk... -->
@else
    <!-- The application is using some other disk... -->
@enddisk
    </div>
@elsedisk('s3')
    <!-- The application is using the s3 disk... -->
@else
    <!-- The application is using some other disk... -->
@enddisk`).trim(), `@disk("local")
    <div>
        @disk("local")
            @disk("local")
                <!-- The application is using the local disk... -->
            @elsedisk("s3")
                <!-- The application is using the s3 disk... -->
            @else
                <!-- The application is using some other disk... -->
            @enddisk
        @elsedisk("s3")
            <!-- The application is using the s3 disk... -->
        @else
            <!-- The application is using some other disk... -->
        @enddisk
    </div>
@elsedisk("s3")
    <!-- The application is using the s3 disk... -->
@else
    <!-- The application is using some other disk... -->
@enddisk`);
    });
});
//# sourceMappingURL=formatter_speculative_conditions.test.js.map