"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const assert_1 = __importDefault(require("assert"));
const utils_1 = require("../formatting/prettier/utils");
suite('Pint Transformer: Speculative Unless', () => {
    test('pint:it can format detected custom unless', () => {
        assert_1.default.strictEqual((0, utils_1.formatBladeStringWithPint)(`@unlessdisk ('local1')
            <!-- The application is not using the local disk1... -->
            @unlessdisk ('local2')
                <!-- The application is not using the local disk2... -->
                @unlessdisk ('local3')
                <!-- The application is not using the local disk3... -->
                @unlessdisk ('local4')
                <div>
                <p>Hello   {{ $world }}</p>
            </div>
            @enddisk
            @enddisk
            @enddisk
            @enddisk`).trim(), `@unlessdisk('local1')
    <!-- The application is not using the local disk1... -->
    @unlessdisk('local2')
        <!-- The application is not using the local disk2... -->
        @unlessdisk('local3')
            <!-- The application is not using the local disk3... -->
            @unlessdisk('local4')
                <div>
                    <p>Hello {{ $world }}</p>
                </div>
            @enddisk
        @enddisk
    @enddisk
@enddisk`);
    });
});
//# sourceMappingURL=formatter_pint_speculative_unless.test.js.map