"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const assert_1 = __importDefault(require("assert"));
const utils_1 = require("../formatting/prettier/utils");
suite('Conditional Element If Statements', () => {
    test('it can detect and format conditional if statements as HTML tags', () => {
        assert_1.default.strictEqual((0, utils_1.formatBladeString)(`                        <@if($something)here @endif class="something">
    <div><p>SOme {{ $text }} text</p>
                            <@if($something)here @endif class="something">
    <div><p>SOme {{ $text }} text</p>
</div>
        </@if($something)here @endif>
</div>
        </@if($something)here @endif>`).trim(), `<@if($something)here @endif class="something">
    <div>
        <p>SOme {{ $text }} text</p>
        <@if($something)here @endif class="something">
            <div><p>SOme {{ $text }} text</p></div>
        </@if($something)here @endif>
    </div>
</@if($something)here @endif>`);
    });
    test('it does not format exact matches as dynamic HTML', () => {
        assert_1.default.strictEqual((0, utils_1.formatBladeString)(`                       <@if($something)here @endif class="something">
            <div><p>SOme {{ $text }} text</p>
                                    <@if($something)here @endif class="something">
            <div><p>SOme {{ $text }} text</p>
        
            @if($something)here @endif
        </div>
                </@if($something)here @endif>
        </div>
                </@if($something)here @endif>`).trim(), `<@if($something)here @endif class="something">
    <div>
        <p>SOme {{ $text }} text</p>
        <@if($something)here @endif class="something">
            <div>
                <p>SOme {{ $text }} text</p>

                @if ($something)
                    here
                @endif
            </div>
        </@if($something)here @endif>
    </div>
</@if($something)here @endif>`);
    });
});
//# sourceMappingURL=formatter_conditional_element_if.test.js.map