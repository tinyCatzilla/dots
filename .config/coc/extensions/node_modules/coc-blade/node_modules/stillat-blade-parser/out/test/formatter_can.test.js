"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const assert_1 = __importDefault(require("assert"));
const utils_1 = require("../formatting/prettier/utils");
suite('Can Directive Formatting', () => {
    test('it can format can directive without errors', () => {
        const template = `{{-- foo.blade.php --}}

@props([
"model",
])

@can('update', $model)
update
@else
any
@endcan`;
        const expected = `{{-- foo.blade.php --}}

@props([
    "model",
])

@can('update', $model)
    update
@else
    any
@endcan`;
        assert_1.default.strictEqual((0, utils_1.formatBladeString)(template).trim(), expected.trim());
    });
});
//# sourceMappingURL=formatter_can.test.js.map