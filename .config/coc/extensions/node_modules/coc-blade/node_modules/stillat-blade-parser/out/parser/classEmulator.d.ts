import { ClassStringRuleEngine } from '../formatting/classStringsConfig';
import { ILabeledRange } from '../nodes/labeledRange';
export declare class ClassEmulator {
    private phpStructuresAnalyzer;
    private jsStructuresAnalyzer;
    private stringParser;
    private mergeRanges;
    private charsToAvoid;
    private classRuleEngine;
    private foundAnyStrings;
    constructor(rules: ClassStringRuleEngine);
    setAllowedMethodNames(allowedMethodNames: string[]): void;
    setExcludedLanguageStructures(structures: string[]): void;
    withAdditionalRanges(mergeRanges: ILabeledRange[]): void;
    private isSafeToProcess;
    getFoundAnyStrings(): boolean;
    emulateString(content: string): string;
    emulatePhpNode(content: string): string;
    emulatePhpTag(content: string): string;
    emulateJavaScriptString(content: string): string;
}
//# sourceMappingURL=classEmulator.d.ts.map